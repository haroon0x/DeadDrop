[project]
name = "server"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.13"
dependencies = [
    "fastapi",
    "httpx",
    "jinja2",
    "psycopg[binary]",
    "pytest",
    "python-multipart",
    "sqlalchemy",
    "uvicorn[standard]",
]
