services:
  - type: web
    name: deaddrop
    runtime: docker
    dockerfilePath: server/Dockerfile
    dockerContext: server
    envVars:
      - key: OWNER_TOKEN
        sync: false
      - key: WORKER_TOKEN
        sync: false
      - key: SECURE_COOKIES
        value: true
      - key: DATABASE_URL
        sync: false
