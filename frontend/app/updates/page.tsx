import type { Metadata } from "next";

export const metadata: Metadata = { title: "Updates" };
const updates = [["Reliability foundation","Agent jobs became recoverable units of work.","Added attempts, sixty-second leases, five-second heartbeats, cancellation, stale recovery, and idempotent terminal writes.","server · worker · database"],["Evidence layer","Receipts stopped trusting agent claims.","Changed files now come from Git, verification comes from configured worker commands, and status comes from observed exit state.","receipts · verification"],["Source isolation","Every job moved into a disposable worktree.","Jobs start from source HEAD, preserve dirty and untracked files, capture binary patches, and remove their temporary worktree.","git · safety"],["Open-source distribution","The repository gained a real operator path.","Durable Compose deployment, migrations, worker setup, release binaries, checksums, contributor docs, support, and security reporting.","operations · community"]];
export default function Updates(){return <><section className="page-hero"><p className="eyebrow">Project updates</p><h1>Building the boring parts until they become the product.</h1><p>A public record of reliability, distribution, and interface work.</p></section><section className="update-list">{updates.map(([label,title,copy,tags])=><article key={label}><div><span>Current</span><small>{label}</small></div><div><h2>{title}</h2><p>{copy}</p><small>{tags}</small></div></article>)}</section></>}
